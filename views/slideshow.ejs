<link rel="stylesheet" type="text/css" href="/<%= deck._id %>/template.css" /> 
<% title = deck.title %>

<% 
	var slides = [];
	if (typeof slideID != "undefined") {
		for (var i = 0; i < deck.slides.length; i++) {
			if (deck.slides[i]._id == slideID) {
				slides = [deck.slides[i]];
			}
		}
	}
	
	if (!slides.length) {
		slides = deck.slides;
	}
%>

<%- partial('slide', slides) %>
